use flake

# Machine-local overrides (not committed).
if [[ -f .envrc.local ]]; then
  source .envrc.local
fi

# Modular environment fragments in .envrc.d/*.sh (sorted).
if [[ -d .envrc.d ]]; then
  for frag in .envrc.d/*.sh; do
    [[ -f "$frag" ]] && source "$frag"
  done
fi
